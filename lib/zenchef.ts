/** Identifiant restaurant Zenchef (Paillote Paradise). */
export const ZENCHEF_RESTAURANT_ID = '386088';

/** Délai d’ouverture auto du widget (ms), cf. configuration Zenchef. */
export const ZENCHEF_AUTO_OPEN_MS = '2000';

/** Couleur principale (hex sans #), cf. interface Zenchef. */
export const ZENCHEF_PRIMARY_COLOR = '2e3345';

/** Langue du widget */
export const ZENCHEF_LANG = 'fr';

/**
 * ID salle « Transat » dans Zenchef (Paramètres → salles).
 * Laisser vide pour ouvrir le widget sans présélection.
 */
export const ZENCHEF_ROOM_TRANSAT = '';

export const ZENCHEF_SDK_URL = 'https://sdk.zenchef.com/v1/sdk.min.js';
